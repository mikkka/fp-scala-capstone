package observatory

import org.scalatest.FunSuite

trait ExtractionTest extends FunSuite {
/*  test("an Extraction locateTemperatures should work somehow on 2000 ") {
    val res = Extraction.locateTemperatures(2000, "/stations.csv", "/2000.csv")
    println(res.take(100))
    println(res.size)
  }

  test("an Extraction locationYearlyAverageRecords should work somehow on locateTemperatures 2000 ") {
    val res = Extraction.locationYearlyAverageRecords(Extraction.locateTemperatures(2000, "/stations.csv", "/2000.csv"))
    println(res.take(100))
    p rintln(res.size)
  }*/
}